#To eshtablish a connection
ssh pi@10.106.61.107         syntax: ssh username@[IP_ADDRESS]
or
ssh pi@raspberrypi.local       syntax: ssh username@YOUR_HOSTNAME.local
    # if the second command doesn't work then use this command
        command: ssh-keygen -R raspberrypi.local
NOTE:username="pi"
     password="raspberry"

#to check temp&throttle
watch -n 2 vcgencmd measure_temp
watch -n 2 vcgencmd get_throttled


#continue for camera 
sudo apt update
sudo apt install -y python3-picamera2 python3-opencv
sudo apt install -y python3-opencv python3-numpy
sudo apt install -y python3-picamera2 python3-opencv python3-tk
sudo apt install -y python3-pil python3-pil.imagetk

#RPI Connect setup
sudo apt install rpi-connect
rpi-connect on
rpi-connect signin


#Connecting RPI to a new network while the old network is there
sudo nmcli dev wifi list
sudo nmcli --ask dev wifi connect 'Goonbuddies' password afnaan123 name "Gooning"          #for a password w/ spaces use "", ex: "afnaan 123"
                            or
sudo nmcli --ask dev wifi connect 'Goonbuddies' password afnaan123 name "Gooning" hidden yes     # if ur wifi is hidden
ctrl + D       # to disconnect w/ rpi
(also, switch off ur network)
nmcli --fields autoconnect-priority,name connection        # to check all connections priority list
sudo nmcli connection modify "Gooning" connection.autoconnect-priority 25    #or whichever no. is higher
